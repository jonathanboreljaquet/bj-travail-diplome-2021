QUESTION M.MATHIEU
-Time slot forcément pour un planning donc "not null" ?
-Est ce que utiliser le VERB delete pour le planning alors qu'on modifie uniquement is_deleted à 1 donc un update ?
-Comment identifier et récupérer les différentes images de l'application depuis l'API

Section sur l'api
-Comment respecter l'architecture REST mais ajouter/modifier/supprimer un chien ou un document à un utilisateur ? 
--> ID dans le body
-Créer plusieurs routes pour chaque type de document PDF ? (condition_inscription, facture)
--> Une seul route


schedule_override
datetime -> date

Format ics envoyé par mail au client

Authentification d’API via JWT et les Cookies
https://website.simplx.fr/blog/2016/09/27/authentification-api-via-jwt-et-cookies/

retourner les permissions en brut lors du endpoint de connexion 

Modification du endpoint `GET api/v1/dogs/downloadPicture` (retourne directement image)
-MODIFIER documentation du endpoint
-MODIFIER tests unitaires postman et dans la doc
Modification du endpoint `POST api/v1/users` (retourne mtn api token + code role)
-MODIFIER documentation du endpoint
-MODIFIER tests unitaires postman et dans la doc
Modification du endpoint `GET api/v1/users` (retourner avec les chiens)
-MODIFIER documentation du endpoint
-MODIFIER tests unitaires postman et dans la doc
Modification du endpoint `GET api/v1/users/{userId]` (retourner avec les chiens et documents)
-MODIFIER documentation du endpoint
-MODIFIER tests unitaires postman et dans la doc
